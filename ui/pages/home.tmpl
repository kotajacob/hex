{{define "main"}}
	<header>
		<h1><a href="/">diet hexbear</a></h1>
		<aside>{{ .MOTD }}</aside>
	</header>
	<hr>
	<main>
		<div class="posts">
		{{ range .Posts }}
			<a href="{{if .URL}}{{.URL}}{{else}}/post/{{.ID}}{{end}}">{{.Name}}</a>
		{{ end }}
		</div>
	</main>
{{end}}
